{% extends 'base.html.twig' %}

{% block title %}Expenses{% endblock %}

{% block body %}
    <h1>Expenses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expense.Name }}</td>
            </tr>
            <tr>
                <th>Worth</th>
                <td>{{ expense.Worth }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ expense.Date ? expense.Date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('expenses_index') }}">back to list</a>

    <a href="{{ path('expenses_edit', {'id': expense.id}) }}">edit</a>

    {{ include('expenses/_delete_form.html.twig') }}
{% endblock %}
